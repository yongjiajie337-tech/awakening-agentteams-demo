# TEMPLATE ONLY. This file contains no credential and is never loaded directly.
# In a prepared reference workspace, create the protected runtime file at:
#   .secrets/demo-provider.env
AWAKENING_DEMO_PROVIDER_API_KEY=<operator-supplied-value>
