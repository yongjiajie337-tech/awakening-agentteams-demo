{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://awakening.local/schemas/m4/state-mcp/apply-authorized-change.params.schema.json",
  "title": "M4 State MCP apply_authorized_change ID-only parameters",
  "type": "object",
  "additionalProperties": false,
  "required": ["program_id", "proposal_id", "expected_state_version", "idempotency_key"],
  "properties": {
    "program_id": {"type": "string", "format": "uuid"},
    "proposal_id": {"type": "string", "format": "uuid"},
    "expected_state_version": {"type": "integer", "minimum": 0},
    "idempotency_key": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"
    },
    "human_decision_id": {"type": "string", "format": "uuid"}
  }
}
